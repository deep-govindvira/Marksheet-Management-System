events {}

http {

  client_max_body_size 200M;

  server {
      listen 80;

      server_name mms.arvrserver.co.in
                  mmsbe.arvrserver.co.in
                  mmsml.arvrserver.co.in;

      return 301 https://$host$request_uri;
  }

  server {
      listen 443 ssl;

      server_name mms.arvrserver.co.in;

      ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
      ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

      location / {
          proxy_pass http://mms-frontend:3000;
      }
  }

  server {
      listen 443 ssl;

      server_name mmsbe.arvrserver.co.in;

      ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
      ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

      location / {
        proxy_pass http://mms-backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      }

      location /api/stream {
        proxy_pass http://mms-backend:8080;
        proxy_http_version 1.1;
        # prevent buffering (CRITICAL for SSE)
        proxy_buffering off;
        proxy_cache off;
        gzip off;

        # keep connection alive
        proxy_set_header Connection '';
        # forward auth header (IMPORTANT for your case)
        proxy_set_header Authorization $http_authorization;

        # SSE stability
        proxy_set_header X-Accel-Buffering no;
      }

  }

  server {
      listen 443 ssl;

      server_name mmsml.arvrserver.co.in;

      ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
      ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

      location / {
          proxy_pass http://mms-ml:8000;
      }
  }

}


# events {}

# http {

#     # Allow large uploads (marksheets)
#     client_max_body_size 200M;

#     # Redirect HTTP → HTTPS
#     server {
#         listen 80;

#         server_name
#             mms.arvrserver.co.in
#             mmsbe.arvrserver.co.in
#             mmsml.arvrserver.co.in;

#         return 301 https://$host$request_uri;
#     }

#     #########################################
#     # FRONTEND
#     #########################################

#     server {
#         listen 443 ssl;

#         server_name mms.arvrserver.co.in;

#         ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
#         ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

#         location / {

#             proxy_pass http://mms-frontend:3000;

#             proxy_http_version 1.1;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

#         }
#     }

#     #########################################
#     # BACKEND
#     #########################################

#     server {
#         listen 443 ssl;

#         server_name mmsbe.arvrserver.co.in;

#         ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
#         ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

#         location / {

#             proxy_pass http://mms-backend:8080;

#             proxy_http_version 1.1;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

#             # Required for auth headers
#             proxy_set_header Authorization $http_authorization;

#         }

#         #########################################
#         # SSE STREAM (CRITICAL)
#         #########################################

#         location /api/stream {

#             proxy_pass http://mms-backend:8080;

#             proxy_http_version 1.1;

#             # Disable buffering (MANDATORY for SSE)
#             proxy_buffering off;
#             proxy_cache off;
#             gzip off;

#             # Keep connection alive
#             proxy_set_header Connection '';

#             proxy_set_header Host $host;
#             proxy_set_header Authorization $http_authorization;

#             # Required for SSE stability
#             proxy_set_header X-Accel-Buffering no;

#         }
#     }

#     #########################################
#     # ML SERVICE
#     #########################################

#     server {
#         listen 443 ssl;

#         server_name mmsml.arvrserver.co.in;

#         ssl_certificate /etc/nginx/certs/mms.arvrserver.co.in+2.pem;
#         ssl_certificate_key /etc/nginx/certs/mms.arvrserver.co.in+2-key.pem;

#         location / {

#             proxy_pass http://mms-ml:8000;

#             proxy_http_version 1.1;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

#         }
#     }

# }